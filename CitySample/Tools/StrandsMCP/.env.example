# Strands Agent Orchestrator configuration (.env)
# Copy this file to ".env" in the same directory and fill in values.

# AWS region for SQS/S3 operations
AWS_REGION=us-west-2

# SQS FIFO queue URL for agent invocations (create queue first)
# Example: https://sqs.us-west-2.amazonaws.com/123456789012/strands-agent.fifo
SQS_QUEUE_URL=

# Optional S3 bucket to upload results and artifacts (JSON summary, screenshots)
# Leave empty to skip S3 uploads
S3_RESULTS_BUCKET=

# MCP server URL (the local FastMCP server that exposes Unreal tools)
# Default points to server.py running locally
MCP_URL=http://localhost:8000/mcp

# Orchestrator concurrency (number of worker threads)
CONCURRENCY=1

# SQS long-poll wait time seconds (0..20)
SQS_WAIT_TIME=20

# Visibility timeout extension seconds while a job is running
SQS_VISIBILITY_EXTENSION_SEC=30

# Small delay between message processing steps
POLL_SLEEP=1.0
